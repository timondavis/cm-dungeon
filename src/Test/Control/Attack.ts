import 'mocha';
import { expect } from "chai";
import { DieBag } from "cm-check/lib/Die/DieBag";
import { Character } from "../../Model/Character";

describe( 'Attack', () => {

    it( 'should facilitate an attack from one character to another (simple)', () => {

    });
});
